//Autoboxing in JavaScript is the automatic temporary conversion of primitive values into their corresponding wrapper objects when we access methods or properties on them.

const userName = "Ranjith";

console.log(userName.length);
console.log(userName.toUpperCase());

let num = 123;

console.log(num.toString());
console.log(typeof(num.toString()));

let bool = true;

console.log(bool.toString());
console.log(typeof(bool.toString()));